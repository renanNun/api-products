version: '3.9'

services:
    cache:
        image: redis
        command: redis-server --appendonly yes
        hostname: redis
        ports:
            - 6379:6379
        volumes:
            - ./data/redis:/data